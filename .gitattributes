# 行尾规范：bat 必须 CRLF（cmd 解析），sh 必须 LF（bash 报 \r 错误）
*.bat text eol=crlf
*.sh text eol=lf
*.ps1 text eol=crlf

# pre-commit 钩子（无扩展名）：必须 LF——CRLF 下 shebang/参数解析异常
pre-commit text eol=lf
.nvmrc text eol=lf

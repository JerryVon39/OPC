<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.CmsPageMapper">

    <resultMap type="CmsPage" id="CmsPageResult">
        <result property="pageId"     column="page_id"     />
        <result property="pageKey"    column="page_key"    />
        <result property="pageName"   column="page_name"   />
        <result property="sort"       column="sort"        />
        <result property="status"     column="status"      />
        <result property="menuPos"    column="menu_pos"    />
        <result property="createBy"   column="create_by"   />
        <result property="createTime" column="create_time" />
        <result property="updateBy"   column="update_by"   />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectCmsPageVo">
        select page_id, page_key, page_name, sort, status, menu_pos, create_by, create_time, update_by, update_time
        from cms_page
    </sql>

    <select id="selectCmsPageList" parameterType="CmsPage" resultMap="CmsPageResult">
        <include refid="selectCmsPageVo"/>
        <where>
            <if test="pageKey != null and pageKey != ''"> and page_key = #{pageKey}</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
        </where>
        order by sort asc, page_id asc
    </select>

    <select id="selectCmsPageByPageId" parameterType="Long" resultMap="CmsPageResult">
        <include refid="selectCmsPageVo"/>
        where page_id = #{pageId}
    </select>

    <select id="selectCmsPageByPageKey" parameterType="String" resultMap="CmsPageResult">
        <include refid="selectCmsPageVo"/>
        where page_key = #{pageKey}
    </select>

    <insert id="insertCmsPage" parameterType="CmsPage" useGeneratedKeys="true" keyProperty="pageId">
        insert into cms_page (page_key, page_name, sort, status, create_by, create_time)
        values (#{pageKey}, #{pageName}, #{sort}, #{status}, #{createBy}, sysdate())
    </insert>

    <update id="updateCmsPage" parameterType="CmsPage">
        update cms_page
        <trim prefix="SET" suffixOverrides=",">
            <if test="pageName != null">page_name = #{pageName},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="menuPos != null">menu_pos = #{menuPos},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where page_id = #{pageId}
    </update>

    <delete id="deleteCmsPageByPageId" parameterType="Long">
        delete from cms_page where page_id = #{pageId}
    </delete>

    <!-- 删除页面时连带删除该页全部区块（含历史由 cms_block 删除逻辑级联） -->
    <delete id="deleteCmsBlockByPageKey" parameterType="String">
        delete from cms_block where page_key = #{pageKey}
    </delete>
</mapper>

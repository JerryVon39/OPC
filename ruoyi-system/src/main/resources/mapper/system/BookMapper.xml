<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.BookMapper">
    
    <resultMap type="Book" id="BookResult">
        <result property="bookId"    column="book_id"    />
        <result property="bookName"    column="book_name"    />
        <result property="author"    column="author"    />
        <result property="bookType"    column="book_type"    />
        <result property="publisher"    column="publisher"    />
        <result property="price"    column="price"    />
        <result property="publishDate"    column="publish_date"    />
        <result property="stock"    column="stock"    />
        <result property="status"    column="status"    />
        <result property="remark"    column="remark"    />
        <result property="cover"     column="cover"     />
        <result property="isbn"      column="isbn"      />
        <result property="intro"     column="intro"     />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="borrowCount"    column="borrow_count"    />
    </resultMap>

    <sql id="selectBookVo">
        select book_id, book_name, author, book_type, publisher, price, publish_date, stock, status, remark, cover, isbn, intro, create_by, create_time, update_by, update_time from book
    </sql>

    <!-- 列表用：额外带借阅次数（借阅记录实时统计），支持"借阅最多"排序 -->
    <sql id="selectBookListVo">
        select book_id, book_name, author, book_type, publisher, price, publish_date, stock, status, remark, cover, isbn, intro, create_by, create_time, update_by, update_time,
        (select count(1) from borrow_record b where b.book_id = book.book_id) as borrow_count
        from book
    </sql>

    <select id="selectBookList" parameterType="Book" resultMap="BookResult">
        <include refid="selectBookListVo"/>
        <where>  
            <if test="bookName != null  and bookName != ''"> and book_name like concat('%', #{bookName}, '%')</if>
            <if test="author != null  and author != ''"> and author = #{author}</if>
            <if test="bookType != null  and bookType != ''"> and book_type = #{bookType}</if>
            <if test="publisher != null  and publisher != ''"> and publisher = #{publisher}</if>
            <if test="price != null "> and price = #{price}</if>
            <if test="publishDate != null "> and publish_date = #{publishDate}</if>
            <if test="stock != null "> and stock = #{stock}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
            <if test="keyword != null  and keyword != ''"> and (book_name like concat('%', #{keyword}, '%') or author like concat('%', #{keyword}, '%') or isbn like concat('%', #{keyword}, '%'))</if>
        </where>
    </select>
    
    <select id="selectBookByBookId" parameterType="Long" resultMap="BookResult">
        <include refid="selectBookVo"/>
        where book_id = #{bookId}
    </select>

    <select id="selectBookByBookIdForUpdate" parameterType="Long" resultMap="BookResult">
        <include refid="selectBookVo"/>
        where book_id = #{bookId} for update
    </select>

    <insert id="insertBook" parameterType="Book" useGeneratedKeys="true" keyProperty="bookId">
        insert into book
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bookName != null and bookName != ''">book_name,</if>
            <if test="author != null">author,</if>
            <if test="bookType != null">book_type,</if>
            <if test="publisher != null">publisher,</if>
            <if test="price != null">price,</if>
            <if test="publishDate != null">publish_date,</if>
            <if test="stock != null">stock,</if>
            <if test="status != null">status,</if>
            <if test="remark != null">remark,</if>
            <if test="cover != null">cover,</if>
            <if test="isbn != null">isbn,</if>
            <if test="intro != null">intro,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="bookName != null and bookName != ''">#{bookName},</if>
            <if test="author != null">#{author},</if>
            <if test="bookType != null">#{bookType},</if>
            <if test="publisher != null">#{publisher},</if>
            <if test="price != null">#{price},</if>
            <if test="publishDate != null">#{publishDate},</if>
            <if test="stock != null">#{stock},</if>
            <if test="status != null">#{status},</if>
            <if test="remark != null">#{remark},</if>
            <if test="cover != null">#{cover},</if>
            <if test="isbn != null">#{isbn},</if>
            <if test="intro != null">#{intro},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateBook" parameterType="Book">
        update book
        <trim prefix="SET" suffixOverrides=",">
            <if test="bookName != null and bookName != ''">book_name = #{bookName},</if>
            <if test="author != null">author = #{author},</if>
            <if test="bookType != null">book_type = #{bookType},</if>
            <if test="publisher != null">publisher = #{publisher},</if>
            <if test="price != null">price = #{price},</if>
            <if test="publishDate != null">publish_date = #{publishDate},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="cover != null">cover = #{cover},</if>
            <if test="isbn != null">isbn = #{isbn},</if>
            <if test="intro != null">intro = #{intro},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where book_id = #{bookId}
    </update>

    <delete id="deleteBookByBookId" parameterType="Long">
        delete from book where book_id = #{bookId}
    </delete>

    <delete id="deleteBookByBookIds" parameterType="String">
        delete from book where book_id in
        <foreach item="bookId" collection="array" open="(" separator="," close=")">
            #{bookId}
        </foreach>
    </delete>

    <!-- 原子扣减库存：单条 UPDATE 条件判断，并发安全 -->
    <update id="updateStock">
        update book set stock = stock - #{quantity}
        where book_id = #{bookId} and stock >= #{quantity}
    </update>

    <!-- 回补库存（归还/取消订单） -->
    <update id="restoreStock">
        update book set stock = stock + #{quantity}
        where book_id = #{bookId}
    </update>

    <!-- 上下架状态切换（后台列表开关）：仅改 status，字段由 Service 层做预约联动校验后调用 -->
    <update id="updateBookStatus">
        update book set status = #{status}, update_time = sysdate()
        where book_id = #{bookId}
    </update>

    <!-- 同类图书推荐：同分类在架书（排除自身，库存充足优先，最多4本） -->
    <select id="selectRelatedBooks" resultMap="BookResult">
        <include refid="selectBookVo"/>
        where book_id != #{bookId} and book_type = #{bookType} and status = '0'
        order by stock desc, book_id asc
        limit 4
    </select>

    <!-- 搜索联想（匿名）：在架书按书名模糊匹配，最多 8 条 -->
    <select id="selectSuggestBooks" resultMap="BookResult">
        select book_id, book_name, author, book_type, price from book
        where status = '0' and book_name like concat('%', #{keyword}, '%')
        order by book_id asc
        limit 8
    </select>
</mapper>
# ============================================
# Docker 构建上下文排除（根目录构建上下文）
# ============================================
# Git
.git
.github

# 本地密钥/口令配置（含真实邮箱授权码，绝不能进入构建上下文）
.env
.env.*

# 后端构建产物与本地依赖
**/target
**/*.jar
**/*.original

# 前端构建产物与依赖
**/node_modules
**/dist
**/npm-debug.log*
**/yarn-error.log*

# 日志与临时文件
**/logs
*.log
**/*.log
.DS_Store
Thumbs.db

# Python 测试脚本缓存（非构建所需）
**/__pycache__
**/*.pyc

# 运行时数据（Redis 持久化文件等）
dump.rdb

# Git 钩子（本机行为，不入镜像）
.githooks/

# 数据库备份
backup/

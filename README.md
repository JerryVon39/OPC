# 🏪 万事屋 · 图书管理系统

> 基于 **RuoYi-Vue 3.9.2**（Spring Boot + Vue 2）从零二次开发的图书借阅 + 购书一体化系统
> 后台面向管理员 · 前台"万事屋"面向读者 · 完整业务闭环 · 学习与演示用

---

## ✨ 功能全景

### 🛍️ 书店前台「万事屋」（匿名可访问，`shop.html`）

- 🔍 **搜索**：书名 / 作者 模糊搜索，分类 Tab 切换，**一键返回全部**（logo / 全部 Tab / 清除筛选）
- ⚡ **搜索联想**：边打字边弹出匹配书名（百度式下拉，防抖 200ms / ↑↓ 选择 / 回车选中），点击联想项直接搜索
- 🔀 **搜索排序**：结果可按"借阅最多 / 最新出版"排序；无结果时显示引导卡片 + **申请荐购**按钮（不再空荡荡）
- 📝 **荐购管理**：读者提交想看的书（书名/作者/附言）→ 后台「荐购管理」处理（待处理/已处理/已拒绝，同书名防重复）
- ⚡ **乐观 UI**：借书 / 续借 / 预约点击瞬间按钮即变（借阅→已借出、续借→已续借、预约→已预约），出错自动回滚；锁粒度按书拆分，并行操作互不阻塞
- 🖼️ **图书展示**：竖版封面卡片（后台可上传真实封面）、在售/下架/库存紧张标签（阈值后台可配）、热门推荐 Top5
- 📖 **图书详情**：基本信息 + 内容简介分区展示，ISBN、价格、库存等完整信息
- 📝 **读者登记**：顾客自助登记，借书证号**后端自动生成**（防伪造/占坑）；**电子邮箱必填**（用于借阅/预约等自动邮件通知，个人中心可改）
- 👤 **读者登录**：姓名 + 借书证号（挂失/停用证号自动拒绝）
- 📚 **借书**：一键借书，应还日期自动 +30 天；**我的借阅**查看剩余天数、**自助续借**
- 🛒 **购书**：购物车（数量增减/移除/合计）→ 一键结算 → 库存自动 -1
- 📦 **我的订单**：按证号/登录态查询，**自助取消订单**（库存自动回补）
- 📢 **公告横幅**：自动轮播 + 长公告跑马灯滚动 + 当日可关闭
- 🎠 **轮播图管理**：后台可增删改前台轮播（上传图片/排序/启用停用，无图渐变背景）
- 📝 **BBCODE 富文本**：简介/公告支持 [b]粗体[/b] [quote]引用[/quote] [url]链接[/url] 等 10 种标签（防 XSS），后台工具栏一键插入
- 📊 **店铺数据条**：横幅下方实时统计（馆藏/读者/今日借出/今日订单）
- 🆕 **新书上架角标**：近 7 天入库的图书显示"新"角标
- 🪟 **弹窗体验**：点击弹窗外空白自动关闭
- 📱 **手机自适应**：≤768px 自动切换移动版布局（导航多行 / 两列卡片 / 弹窗自适应）
- 🎨 **品牌视觉**：墨绿+米金暖木书店调、吸顶导航、胶囊分类、骨架屏加载、页脚三栏
- 🔄 **前后台切换**：前台导航与后台顶栏一键互跳

### 🔧 后台管理（管理员视角）

- 📚 **图书管理**：增删改查、**封面图片上传**、ISBN/简介、在架/下架、库存预警标签、Excel 导入导出
- 🎚️ **上下架开关**：列表状态列拨动开关直接上架/下架（不用进编辑页）；有预约中/可借预约的图书禁止下架（联动校验）
- 📥 **Excel 批量导入**：图书/读者页上传 xlsx 批量新增（模板下载、逐行校验、同名/证号判重跳过、失败行号明细提示；读者证号留空自动生成）
- 🚨 **库存预警**：后台首页自动列出库存低于阈值（book.stock.warn 可配）的在架书提醒补货，点击直达低库存列表
- 👤 **读者管理**：证号自动生成/手填、类型字典、性别/状态、停用/挂失、借阅历史入口、Excel 导出
- 🔄 **借阅管理**：借书/还书/续借、逾期红色标记、按图书/读者筛选、**Excel 导出**
- 📦 **订单管理**：待付款 → **收款** → 完成（仅待付款可取消，取消自动回滚库存），订单删除
- 💰 **逾期罚款**：还书自动结算罚款（0.10 元/天可配）→ 欠费冻结借书 → 收银收款清欠 → 看板欠费总额
- 🪪 **挂失补办**：前台自助申请（姓名+登记手机号，证号丢失也能补办）+ 后台一键补办，旧证号作废、历史快照保留
- 📱 **我的信息**：前台登录后自助修改手机号/邮箱（证号+姓名校验）
- 📧 **自动邮件通知**：借书成功 / 续借成功 / 预约成功 / 预约可借提醒 / 荐购处理结果 自动发邮件（QQ 邮箱 SMTP，异步"尽力而为"不阻断业务，授权码走环境变量 `MAIL_AUTH_CODE` 不进代码库）
- 👤 **登录态优化**：登录后自动隐藏"读者登记"入口，退出恢复
- 📊 **数据看板**：首页 9 项业务统计（含欠费总额）+ 热门图书 Top5 图表 + 快捷入口
- 📈 **借阅统计**：热门图书 Top10、读者借阅排行 Top10
- 🗣️ **公告管理**：催还通知自动发布/回收（见下）
- ♻️ **回收站（误删恢复）**：删除图书/读者先进回收站（含删除人/时间快照），可还原（原 ID/证号被占用自动换新）、彻底删除、清空；有未还借阅/待处理订单/预约中的仍拦截不入站
- 🔐 **角色权限分级**：图书管理员 / 收银专员 / 统计访客 三种角色 + 测试账号（RBAC 完整落地）
- 📚 **差异化借阅规则**：按读者类型区分借阅上限与借期（学生 5 本 30 天 / 教师 10 本 60 天 / 普通 3 本 30 天，参数可配）
- 🛡️ **数据一致性**：借阅/订单快照冗余——删除读者或图书后历史记录依然完整可查

### 🤖 自动化（Quartz 定时任务）

| 任务 | 时间 | 行为 |
|---|---|---|
| 逾期检查 | 每天 0:00 | 自动标记逾期借阅 |
| 逾期催还公告 | 每天 9:00 | 有逾期自动发布催还公告（当天只发一次） |
| 公告回收 | 还书时触发 | 逾期书还清 → 公告自动删除；仍有逾期 → 重新汇总发布 |

## 🧪 内置测试数据（开箱即用）

导入 SQL 后系统自带完整演示数据，**名字即用途**：

### 读者（学生/教师/普通读者 3 类规则 + 挂失/整体/邮箱 演示读者）

| 姓名 | 借书证号 | 类型 | 规则 |
|---|---|---|---|
| 学生测试 | JS20260001 | 学生 | 借阅上限 5 本 / 借期 30 天 |
| 教师测试 | JS20260002 | 教师 | 借阅上限 10 本 / 借期 60 天 |
| 普通测试 | JS20260003 | 普通读者 | 借阅上限 3 本 / 借期 30 天 |
| 挂失测试 | JS20260004 | 普通读者 | 已停用，可演示前台/后台补办 |
| 整体测试 | JS20260005 | 学生 | 演示前台登记/修改手机号 + 数据看板统计全链路 |
| 邮箱测试 | JS20260006 | 学生 | 演示借书/续借/预约/荐购等节点的邮件通知 |
| Jerry | DK | 教师 | 项目作者账号 |

### 后台测试账号（密码均为 `admin123`）

| 账号 | 角色 | 可见范围 |
|---|---|---|
| `admin` | 超级管理员 | 全部（含系统管理/监控/工具） |
| `librarian` | 图书管理员 | 图书/读者/借阅/统计（无订单/系统管理） |
| `cashier` | 收银专员 | 订单 + 借阅办理 + 图书读者查看 |
| `viewer` | 统计访客 | 只读：数据看板 + 借阅统计 |

### 覆盖的演示状态

- **借阅**：借出中 / 即将到期 / 教师 60 天借期 / 已归还 / 已逾期（催还公告演示）
- **预约**：《白夜行》完整状态链——教师测试"可借"、学生测试"预约中"、普通测试"已完成"、Jerry"已取消"（打开前台"📌 我的预约"即见四种状态）
- **订单**：待付款（可取消回滚库存）/ 已收款 / 已完成 / 已取消
- **公告**：逾期催还通知 + 系统上线/维护公告
- **图书**：21 本（文学/科技/历史分类齐全，含 ISBN/简介）

## 🛠️ 技术栈

| 层 | 技术 |
|---|---|
| 后端 | Spring Boot · Spring Security · JWT · MyBatis · Redis · Quartz |
| 前端 | Vue 2 · Element UI · Axios |
| 数据库 | MySQL 8 |
| 部署 | Maven 打包 · Nginx 静态托管（详见部署指南） |

## 🚀 一键启动（Docker，推荐新用户）

从 GitHub 克隆仓库后，**只需安装 Docker**（Windows: Docker Desktop；无需 MySQL/Redis/Java/Node）：

```bash
git clone https://github.com/JerryVon39/book-system.git
cd book-system

# Windows：双击 scripts\start-all.bat，或在命令行运行
scripts\start-all.bat
# Linux/macOS：
./scripts/start-all.sh
```

脚本会自动：检查 Docker → 生成 `.env`（默认配置可直接启动）→ 拉取线上镜像并启动 MySQL/Redis/后端/前端 → 等待后端就绪。

- 管理后台：`http://localhost/`（默认账号 admin / admin123，首次登录请修改密码）
- 读者前台：`http://localhost/shop.html`
- 停止：`scripts\stop-all.bat` / `./scripts/stop-all.sh`（数据保留，重跑 start 即恢复）

> 💡 可选配置：编辑 `.env` 可设置数据库/Redis 口令、JWT 密钥（`TOKEN_SECRET`，生产务必设置）、邮件通知（`MAIL_USERNAME`/`MAIL_AUTH_CODE`，QQ 邮箱 SMTP 授权码）。全部口令默认值仅面向演示，**生产部署务必修改**。
>
> 💡 需要自己改代码时，可 `docker compose up -d --build` 用本地代码重新构建镜像（compose 保留了 build 配置）。

## 🚀 快速开始（本机开发，脚本一键启动）

已有 JDK 17 / Node.js（以及本机 MySQL、Redis，或用 Docker）的开发者，推荐用脚本：

```bash
git clone https://github.com/JerryVon39/book-system.git
cd book-system

# Windows：双击 scripts\start-local.bat，或在命令行运行
scripts\start-local.bat
# Linux/macOS：
./scripts/start-local.sh
```

脚本自动完成：生成 `.env`（默认配置可直接启动）→ 启动/复用 MySQL 与 Redis（本机原生优先，无则自动用 Docker 拉起容器）→ 首次自动建库导入 `sql/` 初始化脚本、已有库自动跑增量升级 → 构建并启动后端（8080）→ 启动前端（8081）→ 打印访问地址。

- 管理后台：`http://localhost:8081/`（默认账号 admin / admin123，首次登录请修改密码）
- 读者前台：`http://localhost:8081/shop.html`
- 前端端口可在 `.env` 用 `FE_PORT` 修改；本机原生 MySQL/Redis 不在 PATH 时可设 `TOOLS_HOME` 指向其安装目录
- 停止：`scripts\stop-local.bat` / `./scripts/stop-local.sh`（数据保留，重跑 start 即恢复）

### 手动启动（可选）

```bash
# 1. 初始化数据库（依次导入；务必带 --default-character-set=utf8mb4，
#    否则 Windows 终端按 GBK 读脚本会把中文读坏入库（菜单乱码/登录报错））
mysql --default-character-set=utf8mb4 -uroot -p < sql/ry_20260417.sql        # 若依系统表
mysql --default-character-set=utf8mb4 -uroot -p < sql/quartz.sql             # 定时任务表
mysql --default-character-set=utf8mb4 -uroot -p < sql/business_init.sql      # 业务表+字典+菜单+订单表（全新库初始化）

# 已有旧库升级到当前版本时，business_init.sql 顶部建表语句在已有库会报"表已存在"，
# 只需额外执行增量升级脚本（幂等）：
mysql --default-character-set=utf8mb4 -uroot -p ry-vue < sql/upgrade_20260818_purchase.sql   # 荐购表+菜单

# 2. 配置数据库连接
#    编辑 ruoyi-admin/src/main/resources/application-druid.yml（账号密码）

# 3. 启动后端
mvn -pl ruoyi-admin -am package -DskipTests
java -jar ruoyi-admin/target/ruoyi-admin.jar

# 4. 启动前端
cd ruoyi-ui
npm install
npm run dev
```

> 💡 **邮件通知**（可选）：新读者登记需填邮箱，借书/预约等自动发信。在项目根目录建 `.env` 文件写入
> `MAIL_AUTH_CODE=你的QQ邮箱授权码` 和 `MAIL_USERNAME=你的发件邮箱`（启动脚本自动读取注入）；不配则邮件不发送、业务不受影响。

- 后台地址：`http://localhost:8081/`（默认账号 admin / admin123）
- 前台万事屋：`http://localhost:8081/shop.html`（顾客视角，无需登录）

> 💡 前端端口在 `ruoyi-ui/vue.config.js` 中配置（默认 80，本机被占用时可改端口）。完整启动/排错细节见《使用指南》。

## 📁 项目结构

```
├── ruoyi-admin        # 启动模块（配置、定时任务 BorrowTask）
├── ruoyi-framework    # 框架层（安全/拦截器/切面）
├── ruoyi-system       # 业务模块（book/reader/borrow/order 的 controller/service/mapper）
├── ruoyi-common       # 公共工具
├── ruoyi-generator    # 代码生成器
├── ruoyi-quartz       # 定时任务框架
├── ruoyi-ui           # 前端（views/system/{book,reader,borrow,order}、public/shop.html）
├── scripts            # 运维脚本（数据库备份 backup-db.bat）
├── docs               # 文档（使用指南/代码生成器流程/部署指南）
└── sql                # 数据库脚本（business_init.sql 业务初始化，幂等）
```

**核心业务代码位置**：
- 借阅业务规则（借书/还书/库存联动/逾期/公告回收）：`ruoyi-system/.../service/impl/BorrowRecordServiceImpl.java`
- 购书业务（校验/下单/库存联动/事务）：`ruoyi-system/.../service/impl/ShopOrderServiceImpl.java`
- 逾期定时任务：`ruoyi-admin/.../quartz/task/BorrowTask.java`
- 书店前台：`ruoyi-ui/public/shop.html`

## 🔐 前台读者登录会话（2026-08-17 加固）

前台读者登录后由后端签发 **短期会话 token**（存 Redis，30 分钟有效），我的借阅/预约/订单查询与借书/续借/取消等操作均需携带 `sessionToken` 并由后端校验归属，不再信任前端提交的借书证号，降低冒用他人证号的风险。未登录时相关入口会提示先登录，不再支持手输证号匿名查询。

## 🛡️ 并发与数据一致性（持续加固）

- 还书、前台/后台取消订单、删除待付款订单全部改为 **原子状态转换**（`WHERE ... AND status = 期望值`），只有状态转换成功的请求才回补库存，并发重复操作不会造成库存虚高；订单号唯一索引（`uk_order_no`）+ 随机后缀，下单不重号；
- （2026-08-18）借书/预约/下单/删除读者以 **FOR UPDATE 锁读者行**（删除图书锁图书行），同一读者的并发操作串行化、检查与写入原子化；配合 **READ_COMMITTED 隔离级别**，加锁后的检查查询读取最新已提交数据——REPEATABLE READ 的旧快照会让并发重复借阅/重复预约漏检、批量删除第 2 个起 id 的检查失真，故这几个写路径显式声明 READ_COMMITTED；
- 单元测试补至 **96 用例**（含并发取消失败不回补库存、加锁后读者被并发删除等场景）。

## 🧪 自动化测试（双保险）

| 层级 | 怎么跑 | 覆盖 |
|---|---|---|
| 单元测试（108 用例） | `mvn test`（秒级，不依赖数据库） | 罚款计算/借阅规则/预约校验/下单校验/订单状态机/删除还原库存/并发取消防双回补/证号生成·查重/统计缓存/BBCODE 渲染/参数回退/FOR UPDATE 行锁路径/并发删除守卫/荐购校验与去重/批量导入判重与错误收集 |
| 集成冒烟（77 项） | `python -u scripts/full_test.py`（需后端已启动） | 前台购物/借阅/预约/订单 + 后台管理全链路 + 搜索联想/排序/荐购/上下架开关 + Excel 导入/库存预警 + 回收站 + 邮件校验 |

> 💡 `python` 命令若被 Windows 应用商店的 python 占位拦截，请用本机 Python 的完整路径运行，或先卸载应用商店占位。

## 🛡️ 部署与运维

- [《部署指南》](docs/部署指南.md)：本机启动 / 正式部署（前端构建 + Nginx 反向代理）/ **cpolar 内网穿透在线演示** / Docker（可选）/ 首次初始化 / 升级回滚
- [《数据库备份脚本》](scripts/backup-db.bat)：一键 mysqldump 备份，可挂 Windows 任务计划定期执行
- [《Docker 发布脚本》](scripts/publish-docker.bat)：一键重新构建 → 推送 Docker Hub → 同步 compose 版本号（`scripts\publish-docker.bat v1.1`）
- **Docker 化已实机验证**：2026-08-18 在本机完成端到端验证（镜像构建 → 四容器编排 → 全新库初始化 → 前后端联通），并修复了 2 个仅在全新库初始化的 SQL 缺陷（详见下文"发展历程"）。日常开发仍用本机环境，Docker 面向服务器部署与环境复现。

## 📖 学习文档

- [《RuoYi-Vue 使用指南》](docs/RuoYi-Vue使用指南.md)：从零学习若依框架的完整手册（17 章：框架概念/功能详解/权限模型/开发流程/前台架构）
- [《代码生成器使用流程》](docs/代码生成器使用流程.md)：10 分钟生成完整业务模块的操作手册

## 🗂️ 发展历程（2026-07 → 2026-08，115 次提交）

> 从若依源码起步，通过"功能迭代 → 审查修复 → 回归测试"循环逐步演进（单元测试 108 用例 / 集成测试 77 项全程护航）。

- **起步与一期**：原始若依源码 → 品牌定制（首页/组织架构/公告）→ 借阅管理 + 借还书 + 库存联动 → 逾期定时任务 + 借阅统计
- **业务规则**：重复借阅 / 借阅上限 / 挂失拦截 → 逾期罚款体系 + 收银收款
- **购书系统**：购物车 / 订单 / 库存联动 / 我的订单 / 自助取消（库存自动回补）
- **前台体验**：万事屋品牌视觉（墨绿+米金）→ 封面上传 / ISBN / 简介 → 搜索与联想 / 排序 / 无结果荐购 → 分类动态化 + 同类推荐 → 收藏 / 公告轮播与跑马灯 / 店铺数据条 / 新书角标 → 轮播图管理 → BBCODE 富文本 → 乐观 UI（点击即变，失败回滚）→ 手机版与中等窗口自适应
- **读者服务**：证号后端生成（防伪造）→ 前台自助登记 / 登录（频控防爆破）→ 挂失补办 / 我的信息 → 图书预约（排队 / 超时自动取消 / 归还通知）→ 短期 Redis 会话 token（全链路校验归属）
- **管理端**：角色权限分级（3 角色 + RBAC）→ 差异化借阅规则（学生/教师/普通）→ 快照冗余（删除后历史完整）→ 回收站（误删还原）→ Excel 批量导入 → 库存预警 → 上下架开关（预约联动校验）→ 数据看板 + 借阅统计（Redis 缓存 + 写路径失效）
- **自动化**：Quartz 三任务（逾期标记 / 催还公告 / 预约超时）→ 异步邮件通知（QQ SMTP，授权码环境变量注入，不进代码库）→ GitHub Actions 自动打包 + Docker 镜像构建
- **工程化**：Docker 四容器编排（MySQL/Redis/后端/前端 + Nginx）→ 一键启动脚本（本机原生优先、自动降级 Docker）→ 数据库备份脚本 → P1-P4 代码质量/性能优化（拆服务、抽常量、加索引）
- **持续安全与并发加固**：库存原子更新（并发不超卖）→ 原子状态转换（防双回补）→ FOR UPDATE 行锁 + READ_COMMITTED（并发串行化、检查与写入原子化）→ 排序字段白名单 / BBCODE 防 XSS / 前台输出转义 → 邮件授权码与发件邮箱全程环境变量化（`.env` 不入库）
- **上传前最终审查（2026-08-20）**：逾期公告按真实日期兜底（不再误删公告）、前台借书事务修复（自调用绕过代理）、借阅页跳转路径修复、下单锁图书行消除下架竞态；同步完成敏感信息清理（历史中真实邮箱/作者邮箱已匿名化）与本文档更新
- **上传前二轮审查（2026-08-20）**：逾期记录编辑放行动态状态（改备注不再误报）、订单取消/删除全路径 CAS 防并发双回补、订单号唯一索引落地（含随机后缀防同毫秒重复）

## 📜 声明

本项目基于开源框架 **RuoYi-Vue**（[gitee.com/y_project/RuoYi-Vue](https://gitee.com/y_project/RuoYi-Vue)）二次开发，遵循 MIT 开源协议。仅用于学习交流。

---

*万事屋 · 图书管理系统 · 学习项目 · 2026*

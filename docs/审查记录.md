# 审查记录 · 数智游民创新工场官网

> 由 `代码审查报告.md` + `审查问题清单.md` + `问题修复清单.md` 三份合并归档（2026-08-24）
> 状态：
> - 第一轮（2026-08-21 发现）：已于 2026-08-24 前全部修复（见各条目 ✅）
> - 第二轮（2026-08-24 审查 `4ccf5fb2..7562b340` 新增功能）：**已全部修复并验证**（R1/F1-F4/M1/M2 于 2026-08-24 修复，M3/M4 已加固；见第五节 ✅）
> - 第三轮（2026-08-24 复查修复提交 `ca1702b5` 后全量代码）：新发现 N1-N7（见第六节）。**N1-N4 已于后续提交修复**（`4291b22e`）；N5-N7 为无害打磨项，当前状态见第六节 ✅/🟢
> - 第四轮（2026-08-24，`fc3e2649` 执行《修改建议》1-4 项后复核）：代码/配置/**文档一致性**专项审查——详见本页「第七节 · 第四轮结论与 push 前清单」
> - 第五轮（2026-08-25，认证邮件大改造与菜单重组/回收站/术语清零之后的全量代码 `fc3e2649..7c67607c`）：4 路并行专项审查（后端 Java／前台静态页／后台管理 Vue／配置·Docker·SQL），新发现 H1-H7 + M 组 + L 组，**均未修复**——详见第八节

---

## 一、修复状态总览

| 批次 | 内容 | 状态 |
|---|---|---|
| ① 功能故障 | 登录/注册参数名、goAdmin 嵌套、home 死代码、遮罩绑定、登录态字段 | ✅ 已修（`4ccf5fb2`） |
| ② 安全收口 | JWT 密钥 fail-fast、PII 快照排除、端口收窄、robots、meta 引号 | ✅ 已修（`4ccf5fb2`） |
| ③ 功能增强 | sessionToken 头化、register/apply 频控、后端文案、esc 补漏 | ✅ 已修（`4ccf5fb2` + 后续） |
| ④ 清理 | toast z-index、sticky、service 100 条上限、dist 重建、SEO 标签 | ✅ 已修（`4ccf5fb2`） |
| ⑤ 个人主页 | 注册即登录、profile.html、入驻申请挂账号、汉堡菜单 | ✅ 已修（`bf752cdc`~`c5d2cd83`） |

## 二、已修复的关键问题（记录备查）

### 功能类
1. **8 页登录/注册参数名 `name` ≠ 后端 `readerName`** —— 复制弹窗时手误，已全部改为 `readerName` ✅
2. **7 页 `goAdmin` 被嵌套进 `submitLogin`** —— 已移到脚本顶层 ✅
3. **home.html 残留 `#bookGrid` 死引用** —— 整段死代码已删，logo 改回顶 ✅
4. **7 页弹窗遮罩绑定失效**（脚本在 DOM 前）—— 改事件委托 ✅
5. **栏目页登录态显示错字段 + 无登出** —— 改 `readerName` + 补 logout ✅

### 安全类
6. **JWT 密钥默认值公开** —— `.env` 写入随机密钥 + 后端启动 fail-fast 拒绝默认值 ✅
7. **pre-commit 快照含 PII** —— 排除 reader/book_purchase_req 两表 ✅
8. **默认口令 + 端口暴露** —— compose 3306/6379 改 expose ✅
9. **CORS 全开放** —— 保留（演示环境可接受，公网部署需收窄）⚠️
10. **补办证可接管他人身份** —— 保留（演示可接受，公网需改发邮箱）⚠️
11. **configKey 匿名无白名单** —— 保留（当前键值无害）⚠️
12. **sessionToken 走 GET** —— 改 X-Session-Token 请求头 ✅
13. **register/apply 无频控** —— 加 IP 频控（失败 5 次/30 分钟）✅

### 体验类
14. **meta description 引号截断** —— 改中文引号 ✅
15. **robots.txt 全站禁抓** —— 放行前台 ✅
16. **service.html 100 条上限** —— 分页循环查找 ✅
17. **toast 层级** —— z-index 1100 ✅
18. **首页硬编码文章链接** —— 动态取政策分类最新 ✅
19. **后端图书术语残留（~40 处）** —— 统一为服务/成员/报名 ✅

### 遗留低优项（可后续处理）
- 死样式清理（8 个零引用 CSS 类，`.related-empty` 保留）
- 共享脚本抽 shared.js（当前手术式修复，未抽）
- 移动端「更多」下拉在超窄屏的布局微调

## 三、架构与技术资产（摘自代码审查报告，仍有参考价值）

### 架构总览
- **后端**：Spring Boot 3 + RuoYi 框架改造，业务模块 `ruoyi-system`（book/reader/borrow/reserve/purchase/cms/banner）
- **前台**：`ruoyi-ui/public/` 纯静态页（12 个 HTML + site.css），fetch 调 `/prod-api` 公开接口
- **后台**：Vue3 管理端（`ruoyi-ui/src/`），登录 admin/admin123（演示）
- **双会话体系**：后台 JWT（Redis 存 LoginUser）+ 前台短期会话（readerSessionService，30 分钟 TTL）

### 核心业务流程
- **报名**：`borrowByCard`（FOR UPDATE 锁 + CAS 条件更新防并发）
- **候补**：`reserveByCard`（名额满排队，`BorrowTask.reserveExpireCheck` 超时自动推进下一位）
- **入驻申请**：`purchase/apply`（登录后自动挂 reader_id，个人主页可查状态）
- **订单**：`ShopOrderServiceImpl`（状态机 0→1→2，加锁顺序 成员→服务）

### 技术实践亮点
- 前台 XSS 防护：全部插值过 `esc()` + 正文 textContent 白名单渲染
- SQL 注入：mapper 全 `#{}`，orderBy 双白名单
- 证号 SecureRandom 8 位不可预测 + 登录防枚举 + IP 频控
- 数据库快照：`.githooks/pre-commit` 自动导出（已排除 PII 表）

## 四、部署要点速查

- **本机开发**：`scripts/start-local.bat`（MySQL + 后端 8080 + 前端 8081 dev server）
- **Docker 部署**：`scripts/start-all.bat`（前端 80 端口，nginx 托管 dist）
- **数据库升级**：`sql/upgrade_2026082*.sql` 按日期顺序执行（幂等）
- **⚠️ 部署前必须重新构建 dist**（`npm run build:prod`），dist 不进 git
- **⚠️ 改动后端代码后必须重新打包并重启 java 进程**——第二轮审查的运行时故障即由"旧 jar + 新页面"版本错位引起（见五-0）

---

## 五、第二轮审查（2026-08-24，范围 `4ccf5fb2..7562b340`）

> 审查对象：个人主页体系、创客大赛页、政策咨询弹窗、A/B 类入驻类型等 12 个 commit 的前后端变更。
> 修复状态（2026-08-24 执行）：**均已修复并验证**。其中 R1 已重打包重启并实测 X-Session-Token 头通过；F1/F2/F3/F4、M1/M2 已改代码/SQL 并在本地库生效；M3（XFF 取末段）、M4（关 Swagger + 去 8080 直暴露）已加固。L3-L8 打磨项本轮未处理（见文末"遗留低优项"）。

### 🔴 运行时故障

**R1. 前后端版本错位 → 所有需要会话的操作报"登录已失效"**（用户实机复现）
- 现象：登录成功、能打开个人主页（本地 localStorage 渲染），但报名/候补/我的报名/我的候补/改资料全部提示"登录已失效，请重新登录"，重新登录无效。
- 根因：运行中的后端是 **08-21 09:48 构建的旧 jar**（早于 `4ccf5fb2` 的会话头改造）；jar 内 `ReaderSessionService` 无 `resolveFromRequest`，只从请求参数读令牌；而新前端只发 `X-Session-Token` 头、body 不带参数 → 后端永远校验失败。
- 实机验证：同一令牌，body 参数方式通过、header 方式被拒，证据确凿。
- 影响接口：`borrow/borrowByCard`、`borrow/queryByCard`、`reserve/add|myList|cancel`、`purchase/myList`、`reader/updateMyInfo`、shopOrder 相关（共 5 个控制器）。
- 修复：重新打包 `mvn -pl ruoyi-admin -am package -DskipTests` 并重启后端；同时确认本地库已执行 `upgrade_20260824_profile.sql`（否则新 jar 起来后入驻申请模块因缺 `reader_id` 列报错，见 F3）。

### 🔴 功能坏死（新功能开箱即坏）

**F1. policy.html 咨询表单邮箱正则丢反斜杠 → 表单基本不可用**
- 位置：`ruoyi-ui/public/policy.html:248`
- `/^[w.+-]+@[w-]+(.[w-]+)+$/` 字符类只匹配字面 `w . + -`；实测 `test@example.com` → false，仅 `www@www.www` 这类能过。正常邮箱全被前端拦截。
- 其余 19 处同款正则均正确，唯此一处漏 `\`。修复：改为 `/^[\w.+-]+@[\w-]+(\.[\w-]+)+$/`。

**F2. 创客大赛「报名参赛」必然失败——bookId=23 不存在**
- 位置：`ruoyi-ui/public/contest.html:147` 写死 `CONTEST_BOOK_ID = 23`；数据库 `book` 表 ID 仅到 22（快照核实），且无任何种子 SQL 创建"创客大赛报名"服务条目，开发文档也未写部署前置步骤。
- 全新 clone + Docker 一键启动路径下该页必坏。修复：补一条种子 SQL（或后台建好后在文档/注释中固化 ID）。

**F3. 全新 Docker 部署缺 `reader_id` 列 → 入驻申请模块 SQL 报错**
- `sql/business_init.sql:441` 建表无 `reader_id` 列；`docker/mysql-init.sh` 初始化清单不含 `upgrade_20260824_profile.sql`（加列脚本）；而 `BookPurchaseReqMapper.xml` 的查询列无条件包含 `reader_id`。
- 后果：全新库初始化后，后台「入驻申请」列表与个人主页「我的入驻申请」Tab 报 `Unknown column 'reader_id'`。老库不受影响（手工跑过升级脚本）。
- 修复：`business_init.sql` 补列定义 + `mysql-init.sh` 补一行执行升级脚本。

**F4. contest.html 漏掉「注册即登录」改动**
- `e4555c9e` 给 10 个页面的注册成功回调加了写入会话（`currentUser = d.data; localStorage.setItem; renderLoginState()`），唯独 contest.html 的 `submitRegister` 成功分支未加。刚注册完点"报名参赛"仍被判未登录弹登录框。

### 🟠 中等（正确性 / 公网加固）

**M1. 挂失补办不同步候补表的证号快照**
- `ReaderServiceImpl.reissueCard()` 只更新了 `borrow_record.card_no`（updateCardNoSnapshot），没动 `book_reserve.card_no`。换新证号后：
  - 个人主页「我的候补」按新证号查 → 旧候补记录消失；
  - `reserveByCard` 重复候补校验按 card_no → 校验失效，可重复候补；
  - `cancelByCard` 用 `reserve.getCardNo()` 做归属比对 → 旧候补无法取消。

**M2. 会话过期被静默吞成"暂无记录"**
- 会话 TTL 30 分钟且无滑动续期。profile.html 的 `loadBorrows/loadReserves/loadPurchases` 与 services.html 的 `queryMyBorrow/queryMyReserves` 均不检查 `d.code`：过期后 data=null 被当空列表渲染成"暂无××记录"，用户误以为数据丢失，而非引导重登。

**M3. 频控 IP 取值可伪造**
- 登录/注册/补办/入驻申请频控基于 `IpUtils.getIpAddr()`（取 XFF 第一段），nginx `$proxy_add_x_forwarded_for` 为追加不覆盖 → 客户端自带伪造 XFF 即可轮换绕过"30 分钟 5 次"。演示可接受；公网部署需改信任 nginx 追加后的最后一段或 `$remote_addr`。⚠️

**M4. 后端 8080 直映射公网 + Swagger 匿名开放**
- compose 发布 `"8080:8080"`，同时 SecurityConfig 对 `/swagger-ui/**`、`/v3/api-docs/**` permitAll 且生产配置 `springdoc.swagger-ui.enabled: true` → 可绕过 nginx 直接浏览全量 API 文档。建议生产关 springdoc 或去掉端口映射。

### 🟡 低（打磨项）

- L1 八个页面 `renderLoginState` 结尾多拼一个孤立 `' + '</span>'`（浏览器容错忽略，复制粘贴残渣）：about/article/industry/join/news/policy/talent/service。
- L2 提交通道不一致：home/services 入驻申请走 `apiFetch`（带会话头，登录后自动挂账号）；join.html:177 与 policy.html:256 走裸 `fetch` → 登录用户提交不关联账号，「我的入驻申请」看不到自己的提交。
- L3 长度校验缺失：`book_name`(100)/`author`(60)/`remark`(255) 前端均无 maxlength，超长直接 DB 500；【A 类】前缀还会再占 remark 若干字符。
- L4 定时任务两处：`BorrowTask.reserveExpireCheck` 把候补推进为"有名额"后不发邮件（returnBook 路径发，行为不一致）；`remindOverdue` 公告文案仍是"图书/读者/办理还书"旧术语（第一轮 #19 清理的漏网）。
- L5 `FineService:45` 使用已废弃的 `BigDecimal.ROUND_HALF_UP`（应改 `RoundingMode.HALF_UP`）。
- L6 services.html 死代码：`authParams`(:227)、`PUBLIC_STATS_API`、`NOTICE_API`、`noticeTexts`、`RELATED_API` 定义后从未使用。
- L7 本地 `ruoyi-ui/dist/` 落后于 `public/`（dist 已 gitignore、Docker 构建时重建；仅本地直接开 dist 预览会看到旧版，易误判）。
- L8 news.html 注册弹窗类型下拉缺"请选择成员类型"空默认项（contest.html 有），两页不一致。

### ✅ 第二轮核对过、确认没问题的

- 第一轮已知接受风险（CORS 开放、补办可接管、configKey 匿名）维持原判；
- 报名/候补/完成的并发控制（FOR UPDATE 加锁顺序统一 成员→服务、CAS 状态推进、库存原子扣减、防双回补）依然严密；
- `.env` gitignore 生效；pre-commit 快照继续排除 PII 两表；JWT 默认密钥 fail-fast 仍生效；
- XSS 防护：12 页 `readerName` 插值全过 `esc()`；article 正文 textContent + URL 白名单方案完好；
- CI（build.yml）跑单测 + 双镜像构建验证正常。

### 建议修复顺序

1. **R1**（重新打包重启后端）+ **F3**（补列）——先让本地恢复可用；
2. **F1 / F2 / F4**——三个新功能坏死项，均为小改动；
3. **M1 / M2**——正确性问题；
4. **M3 / M4**——公网部署前必做；
5. L 组择机清理。

---

## 六、第三轮审查（2026-08-24，对象：修复提交 `ca1702b5` 之后的全量代码）

> 审查方式：代码走查 + 本地新 jar 实机请求验证。
> **修复状态（更新于 2026-08-24 第四轮复核）**：N1-N4 已由 `4291b22e` 修复/加固；N2 设计上由 Docker 构建重建 dist 解决；N5-N7 为无害打磨项，可择机。

### 上轮修复自查结论

第二轮 12 项修复中 11 项经代码 + 实机双重验证正确落地：

- R1：5 个控制器（borrow/reserve/purchase/reader/shopOrder）全部走 `resolveFromRequest`；实测带 `X-Session-Token` 头通过、无头被拒；
- M1 闭环质量好：`reissueCard` 带 `@Transactional` 三步写同一事务；下游三处消费全部核对——重复候补校验按 `card_no+book_id`、`cancelByCard` 归属比对按 `card_no`、候补推进按 `book_id+status` 定位且邮件按 `reader_id` 查成员——换号后均正常；
- F2/F3：init.sh 执行顺序合理，幂等守卫有效，本地库生效且 HEX 校验中文编码正确；
- 其余 F1/F4/M2/M3/M4/L1/L2 均确认落地。

### 🔴 功能坏死（实机复现）

**N1. contest.html「报名参赛」仍必败——报名请求不带任何会话凭证** ✅ 已修复（`4291b22e`）
- 位置：`ruoyi-ui/public/contest.html:271-274`，`doContestBorrow` 用裸 `fetch(BORROW_API)`，body 仅 `{cardNo, bookId}`——既无 `X-Session-Token` 头也无 `sessionToken` 参数。
- 实机验证（对新 jar）：无头请求 → `code:500 登录已失效`；带头请求 → `code:200 报名成功`。
- 因果链：F4 让用户注册后"看起来已登录"（导航显示用户名），点报名却被后端拒——比修复前更迷惑。对照 `service.html:375/395` 与 `services.html:514` 均正确用 `apiFetch`，唯 contest 页漏网。三轮审查层级：R1 查了后端改造 → F4 补了注册回调 → N1 是第三层"报名动作本身"，连续两轮漏查。
- 修法（约 10 行）：仿 service.html 定义 `apiFetch` 并替换该处 fetch。
- 修复确认（2026-08-24）：`4291b22e` 在该请求头补 `X-Session-Token`（`contest.html:276`），实机带头报名返回 `200`。

### 🟡 中低（易误判 / 一致性）

**N2. dist/ 落后且含误导性中间态（L7 恶化）** 🟢 已解决（设计上）
- `dist/contest.html` 构建于修复提交之前（09:11）：无 F4、无 N1 相关改动。本地直接开 dist 预览会看到旧现象，易误判为修复无效。Docker 构建会重建 dist，仅影响本地预览。
- 现状：dist 已 gitignore、不进仓库；Docker/脚本构建时由源码重建。本地预览需手动 `npm run build:prod`，勿直接开历史 dist。

**N3. 操作类接口的过期提示未统一（M2 尾巴）** ✅ 已修复（`4291b22e`）
- `profile.html saveMyInfo`、`services.html updateMyInfo/cancelReserve/renewByCard` 会话失效时仅显示后端原文"登录已失效，请重新登录"——不算静默吞错（文案明确），但不清理本地登录态、不自动弹登录框，与列表查询的 `handleSessionExpired()` 行为不一致。
- 修复确认：`profile.html:334/352/375/398` 与 `services.html:748/812` 已统一在检出 `登录已失效` 时调 `handleSessionExpired()`（清登录态 + 弹登录框）。

### 🟢 低（打磨项）

- N4 单段伪造 XFF 边界残留：`getMultistageReverseProxyIp` 仅在 XFF 含逗号时取末段（IpUtils.java:260），客户端发单段伪造值（如 `X-Forwarded-For: 9.9.9.9`）时原样采信（实测）。生产经 nginx 后 XFF 至少两段、末段必为真实对端，且 M4 已封 8080 直连，实际攻击面已闭合。✅ 已加固（`4291b22e` 补充单段处理）。
- N5 文档与小瑕疵三处：🟢 保留，择机清理
  - `IpUtils.java:250` 方法 javadoc 首行仍是"获得**第一个**非unknown IP地址"，与 `@return` 行"最后一个"自相矛盾；
  - `sql/upgrade_20260824_profile.sql` 末行缺换行符（执行无碍，纯规范）;
  - 前台匿名补办接口 `applyReissue` 在 `public/*.html` 无任何调用入口——功能目前只有后台入口，若预期开放前台自助补办则缺页面（也可能是有意下线，需产品确认）。
- N6/N7 M4 遗留死配置（无害，🟢 保留）：`SecurityConfig.java:106` swagger permitAll 规则仍在（springdoc 已关，路由落到静态资源 404，不泄露内容）；`vue.config.js:59-63` dev proxy 的 `/v3/api-docs` 转发已成死配置。

### ✅ 第三轮核对过、确认没问题的

- 第二轮已知接受风险维持原判；并发控制（FOR UPDATE 锁序、CAS 推进、原子扣减）经 M1 改动后依然严密；
- nginx.conf 确认设置 `$proxy_add_x_forwarded_for`（追加式 XFF），M3 取末段的生效前提成立；
- pre-commit 快照继续排除 PII 两表，快照中 book 表已含 `book_id=23` 创客大赛条目；
- contest 报名成功后无本地状态刷新（无 borrowedIds 缓存），纯提示文案，无不一致风险。

### 第三轮建议修复顺序

1. **N1**——contest.html 报名加会话头（约 10 行，功能坏死）；✅ 已由 `4291b22e` 修复
2. **N2**——重建 dist（消除本地预览误导）；✅ 设计上解决（Docker/脚本重建，dist 不入 git）
3. **N3 / N5**——过期提示统一 + 注释/规范小修；N3 ✅ 已由 `4291b22e` 修复；N5 择机
4. N4/N6/N7 可接受现状或顺手清理；N4 ✅ 已加固，N6/N7 保留（无害）

---

## 七、第四轮审查（2026-08-24，push 前复查）

> 审查对象：`fc3e2649`（执行《修改建议》1-4 项）后的全量代码 + 配置 + 文档一致性。
> 结论：**功能与数据安全无明显阻断项**；发现问题集中在**文档滞后与少量仓库卫生**，已随本次提交同步更新。

### 🔴 高（已随本次推送修复）

- **H1. `scripts/__pycache__/*.pyc` 与空文件 `scripts/print` 被 git 跟踪**——构建产物/垃圾文件不应入库。已 `git rm`（含 `.gitignore` 补 `__pycache__/`、`*.pyc` 规则）。

### 🟠 中（文档滞后，已同步更新）

- **M1. README 定时任务表仍列 4 个任务**——`fc3e2649` 已删公告类 3 个任务，现仅 `reserveExpireCheck`（每天 8:00）。README 已改为 1 项。
- **M2. README 前台导航描述 10 项**——实际 8 项（服务项目/创客大赛已隐藏）。README 已更新并链接《暂隐藏栏目》文档。
- **M3. 开发文档定时任务表 / 后台菜单描述过时**——`remindOverdue` 等任务已删、Excel 导入导出按钮已隐藏。开发文档已同步。
- **M4. 部署指南 Druid 监控台描述与配置矛盾**——指南称"默认开启 ruoyi/123456 允许所有 IP"，实际 `application-druid.yml` 已默认 `DRUID_STAT_ENABLED:false` 关闭。指南已改为"默认关闭，开启务必改口令"。

### 🟢 低（本次未改，建议下一轮择机）

- **L1. `scripts/full_test.py` 引用已删除角色**（viewer/librarian/cashier）——角色精简后该脚本对应用例会因用户不存在而失败。建议更新或移除（`smoke_official.py` 为现行冒烟脚本）。
- **L2. `ip` 处理 javadoc 自相矛盾（N5 遗留）**：`IpUtils.java:250` 首行"第一个" vs `@return`"最后一个"。
- **L3. `sql/upgrade_20260826_policy.sql` 版本号带未来日期**（文件名 v20260826，实际内容 08-21 完成）——仅影响升级顺序可读性，执行按日期排仍正确。
- **L4. SecurityConfig swagger permitAll / vue.config.js `/v3/api-docs` proxy 死配置（N6/N7）**——springdoc 关闭后不可达，可顺手删除。

### ✅ 本次复核确认无问题

- .env（含 MAIL_AUTH_CODE 等）仍 gitignore；dump.rdb/logs/backup/target/node_modules 均未入库；
- 快照 `data_snapshot.sql` 已排除 reader / book_purchase_req 两表；SQL 中手机号为演示占位（13800000xxx 等），邮箱为 ry@qq.com 等演示值，无真实个人数据；
- docker-compose 3306/6379/8080 均仅内网 expose；CI（build.yml）无密钥写入、不上传 .env；`.dockerignore` 排除 .env；
- JWT 默认密钥为随机串 + compose 兜底可被启动自检拒绝，生产仍须 `.env` 设 `TOKEN_SECRET`；
- `admin/admin123` 为若依公开默认演示口令——**公网部署前必须改线上库 admin 密码**（见《部署指南》五-改默认密码）。

---

## 八、第五轮审查（2026-08-25，范围 `fc3e2649..7c67607c` 全量代码）

> 审查方式：4 路并行专项审查——① 后端 Java 自定义代码（12 个 Controller／自定义 Service·impl／12 个 Mapper XML／BorrowTask／SecurityConfig）；② 前台静态页（13 个 HTML + site.js + css，16 个内联脚本 `node --check` 0 失败）；③ 后台管理 Vue（banner/book/borrow/cms/mail/purchase/reader/recycle/reserve + api/router）；④ 配置·Docker·SQL（yml/compose/nginx/22 个 upgrade 脚本/pom/CI）。所有发现均实际读到代码并标注行号，已与前四轮及「已知接受风险」去重。
> **状态：全部 ⬜ 待修复（本轮仅汇总，未动手改任何文件）。**
> 结论先行：未发现 CRITICAL 级问题；主体质量高于典型若依二开水准。问题集中在两类——「结构性变更没同步到引用方」（菜单重组、dist 构建）与「秘密管理的最后一公里」（JWT 密钥回退默认值）。

### 🔴 高（H1-H7）

**H1. JWT 密钥明文回退默认值硬编码在两个被跟踪文件中——README「fail-fast」承诺与实现不符**
- `docker-compose.yml:83` 与 `ruoyi-admin/src/main/resources/application.yml:115` 的 `TOKEN_SECRET` 回退值完全相同且入库；README.md:265 宣称 fail-fast，实际是宽松回退。拿到仓库即可对未设环境变量的部署伪造任意 admin JWT。
- 修复方向：删除两处回退默认值改真 fail-fast（缺失即拒启），并轮换当前密钥值。

**H2. start-all.bat 升级清单过期——Windows 老库升级后前台登录/注册/邮件直接 500**
- `scripts/start-all.bat:66` 内联 SQL 清单缺 `upgrade_20260824_auth.sql` 及 menu_reorg/recycle 三件套，与 `docker/mysql-upgrade.sh:24-42` 完整清单不一致。
- 修复方向：bat 改调 mysql-upgrade.sh，四条执行链共用一份有序清单；顺带去掉吞错误逻辑。

**H3. editor 角色在全新库完全不可用**
- 三重叠加：`upgrade_20260825_menu_reorg.sql:71-80` 隐藏了授予 editor 的旧目录、新建顶层目录无任何脚本授予 editor → 菜单树因父级不在授权列表成孤儿全部丢弃，侧边栏为空；`role_init.sql:19-21` 授予的按钮权限菜单在所有 SQL 中均无创建语句（INSERT..SELECT 匹配不到行静默无效）→ 增删改全 403。admin 因超管旁路一直未发现。
- 修复方向：reorg 后补授新目录给 editor；补建缺失的 CRUD 按钮权限菜单。

**H4. 菜单重组后 4 处硬编码路由跳转全部 404**
- `views/index.vue:37-40`（首页看板快捷入口，含指向早已删除的订单页）、`reader/index.vue:517`、`book/index.vue:540`、`borrow/index.vue:151`（watch 路径永远匹配不上）。
- 修复方向：按新结构 `/member/borrow` 等更新，或改用路由 name 跳转而非硬编码 path。

**H5. CMS 发布权限旁路**
- `cms/index.vue:109-114` 编辑弹窗状态 radio 无条件提供「已发布」——仅 edit 权限角色经 PUT 提交 `status='0'` 即可发布，按钮上的 `system:cms:publish` 形同虚设；后端 `CmsArticleServiceImpl.updateCmsArticle`（92 行）照常放行并补写发布时间。
- 修复方向：radio 按权限过滤选项，或后端 update 校验「草稿→已发布」需 publish 权限。

**H6. dist 陈旧 + 预构建镜像不重建——上线旧站隐患**
- `dist/*` 全部停留在 08-24 09:11 旧一代构建（导航统一/site.js 抽取之前）；`docker-compose.yml:106` 用预构建镜像 `opc-frontend:v2.1` 且不挂载本地 dist——若镜像早于 08-25 改动构建，线上就是旧站；任何手工拷贝 dist 的发布方式必然发旧版。（L7/N2 的恶化形态）
- 修复方向：确认 v2.1 镜像构建时间晚于 08-25 全部改动，否则重跑 `npm run build:prod` 重建推镜像；发布只走 Docker 构建通道。

**H7. 前台两处用户可见死点**
- `talent.html:130` CTA 按钮 `home.html#services` 指向已不存在的锚点（home 04 服务概览整段被注释隐藏），点击跳首页顶部无任何反馈；
- `services.html:840` URL 参数 `?type=` 未转义拼入 querySelector，异常参数抛 SyntaxError 且 then 链无 catch，列表静默空白。
- 修复方向：talent 改指向 join.html 或隐藏按钮；services 加 `CSS.escape()` + 链尾 catch 兜底调 loadBooks()。

### 🟠 中（M 组，按主题分组）

安全/数据完整性：
- **M1 忘记密码账号枚举**：`ReaderController.java:375-385` 把 sendCode 的频控异常文案透传给客户端——返回「发送过于频繁」即证明该证号存在且有邮箱，恒定成功即不存在。catch 改静默记日志 + 统一成功响应。
- **M2 欠费记录可删**：`BorrowRecordServiceImpl.java:533-575` 欠费拦截只包在状态∈{进行中,逾期}分支内，「已完成但罚款未缴」绕过守卫被无条件删除 → 应收罚款凭据消失。把拦截移到状态分支外对所有待删记录统一校验。
- **M3 删栏目产生孤儿文章**：`CmsCategoryServiceImpl.java:64-79` 物理删除栏目前不检查也不迁移旗下文章 → 文章从前台所有 Tab 消失、后台无法找回归属。删前检查 countArticleByCategoryId>0 则拒绝或迁移。
- **M4 上传链路两层裸奔**：nginx 无 client_max_body_size（默认 1MB）vs 后端 max-file-size 10MB → 生产 >1MB 配图一律 413；cms/book/banner 三处 el-upload 无类型/大小校验、无 on-error（HTTP 层失败静默无反馈）。server 块加 20m + 公共 before-upload 校验。
- **M5 stats.vue 孤儿页**：整页无菜单注册、`system:borrow:stats` 权限点全库零种子，页面不可达（URL 手输也 404），即使补菜单非 admin 也无法授权。要么补菜单+权限 SQL 激活，要么删页面与 borrowReaderStats 死代码。

前后台一致性/校验：
- **M6 手机号/密码三处两种标准**：site.js 注册与改资料只查非空 vs profile.html:342 查 11 位 vs 后端强制 `\d{11}`；profile.html:440 改密码只查长度≥10 不查复杂度（与提示文案不符）。收敛进 site.js 单一实现（复用 passwordStrengthOk）。
- **M7 会话过期靠文案匹配**：site.js:105-113 等以 `msg.indexOf('登录已失效')` 判定而非状态码；home.html:679-711 内联版查询函数连此判断都没有，过期后仅显示查询失败。apiFetch 统一按 401/错误码分流 handleSessionExpired。
- **M8 状态 '2' 文案口径错误**：borrow/index.vue:54,127 与 index.vue:85 显示「已截止」，BizStatus.java:22-23 语义是「已逾期」（关联罚款催缴流程），运营会漏掉收罚款动作。
- **M9 费用/名额自由文本输入**：book/index.vue:248-266 price/stock 无数字校验（rules 只有 bookName），非数字 Jackson 反序列化 400，负数名额破坏禁报与预警逻辑（stock 为 null 还显示「仅剩 席」）。改 el-input-number + 校验规则。
- **M10 后台下拉 pageSize:100 封顶**：borrow/index.vue:173、cms/index.vue:166 一次性拉 100 条——成员超百人后代客报名搜不到人（filterable 只在已加载数据内过滤）。改远程搜索 remote-method。
- **M11 回收站列表失败永久转圈**：recycle/book.vue:132-139、recycle/reader.vue:120-127 getList 无 catch/finally——今日刚返工的两页恰是全项目唯二漏错误处理的列表页。对齐其他页面 .finally 写法。

展示/运营：
- **M12 服务详情显示 BBCode 字样**：种子 intro 含 `[b]…[/b][url=#]`，后端 renderBookIntro 预渲染 HTML 与前台 service.html:402 textContent 渲染方式错位 → 标记原样可见（安全性反而无损）。统一为 article 式白名单渲染或录入侧禁 BBCode。
- **M13 canonical 占位域名**：12 页 `<link rel="canonical">` 全指保留域 `opc.example.com`，正式域名上线后收录/权重被引走。按环境注入真实域名。
- **M14 首页 ECharts 泄漏**：views/index.vue mounted 与 loadStats 回调各 init 一次同一 dom，不 dispose、不响应 resize、离开不销毁。缓存实例 + beforeDestroy 销毁 + resize 监听。
- **M15 home 死弹窗 ~90 行 + join 表单缺 A/B 类别**：home.html:293-383 报名/候补/入驻三个弹窗完整保留但无任何入口调用；入驻类型选择只存在于打不开的弹窗里，join.html 主表单没有该字段（TODO 第三批 #9 相关）。删弹窗或把字段补进 join 表单。
- **M16 回收站 SQL 双脚本意图相反**：upgrade_20260825_recycle_cleanup.sql:32-38（删入口，注释"用户确认移除"）与 recycle_restore.sql:15-36（恢复入口，注释"用户确认保留"）同时在执行链中，不同环境终态分叉。废弃其一并在 README 固化权威执行顺序。

### 🟢 低（L 组摘要，择机清理）

- 后端：会话 token 支持 URL 参数兜底（ReaderSessionService.java:29-38，进 nginx 日志/Referer/浏览器历史，注释已标过渡期移除）；匿名分页 pageSize 无上限（TableSupport.java:47，可全表拉取）；CMS publicList 排序无白名单（框架 SqlUtil 白名单兜底不成注入）；DashboardController:30-38 缺 @PreAuthorize（低权账号可见罚款总额等经营数据）；purchase apply 可伪造 createBy 审计字段；会话 by-card 索引整读改写非原子（多端列表可能漏设备）。
- 前台：9 页各约 30 行导航迁移残渣脚本块；policy.html:239/contest.html:218/join.html:177 手工拼 token 头不走 apiFetch 且 policy 无防重复提交锁（双击双条）；localStorage 键名仍叫 shopUser 且存长期凭证；article views/id 数字插值未 esc（防御缺口非漏洞）；css 两代 .home-hero 并存死样式；汉堡菜单同页锚点跳转不收起；svcDoneBtn「已候补」文案不复位。
- 后台：recycle 两页死 import getDicts；recycle/reader.vue:46「挂失」不可达分支（无第三态）；批量删除确认框拼接原始数组（若依祖传）；业务状态硬编码数字未走字典管理（当前值与 BizStatus 核对一致，属维护风险）。
- 工程：POI 4.1.2 / jjwt 0.9.1 偏旧（POI<5.4 有已知 CVE，导出仅登录用户可达风险受控）；logback.xml 写死 `/home/ruoyi/logs`（Windows 写到盘根）且有重复 root 块；start-local.bat 强杀端口进程可能误杀 nssm 服务形成自动重启竞态；start-all/start-local 是两套互不相通的数据库易造成"后台改了前台没变"；install-services.bat GBK 编码乱码；演示数据依赖隐式自增 ID 耦合（book_reserve 引 book_id=15 等）；快照两条同名重复轮播（id2/id4）；dump.rdb 在 git 历史（私有仓可接受，转公开前清理）。

### ⚠️ 对既有已知项的升级认知

**频控 IP 信任链（二轮 M3/三轮 N4 曾判定"攻击面已闭合"，本轮深查后上调）**：登录限速、账号锁定、注册防刷、验证码日配额、入驻防刷**全部**以 XFF 为 key（IpUtils.java:44-68），且 IP 参与账号锁定 key（`failKey = "login:" + ip + ":" + account`，轮换伪造 XFF 连账号级计数也归零）。闭合前提是 nginx 追加式 `$proxy_add_x_forwarded_for` + 8080 不直连公网**同时成立**。公网上线前应改为应用内只信任可信代理写入的那一跳。

### ✅ 本轮核实无问题（避免误报）

- **前台匿名接口无越权**：@Anonymous 接口全部先解析会话再强制 cardNo 归属比对；updateMyInfo 另校验姓名一致、邮箱变更走验证码；purchase readerId 强制置空防栽赃——"凭 cardNo 查改他人数据"不可行；
- **无 SQL 注入**：12 个自定义 Mapper XML 全参数化、模糊查询走 concat(#{}), `${}` 仅剩框架 dataScope 受保护 3 处；
- **并发控制成熟**：原子条件 UPDATE 扣库存、CAS 状态机推进、FOR UPDATE 锁序统一成员→服务、borrowByCard 外层 @Transactional 已规避自调用代理失效；
- **认证改造质量高**：token 约 128 位熵 SecureRandom、Redis 存储 14 天滑动续期绑定 IP/设备、密码重置后 revokeOthers 全端吊销；验证码 60s 节流跨 purpose 共享防轮换 + IP 日配额 + 错 5 次作废；SMTP 授权码 AES-GCM 加密、明文落库被拒、接口永不回显；
- **XSS 主防线完好**：esc 纪律、正文 textContent+链接白名单渲染、banner 协议白名单，当前无可达注入路径；16 内联脚本 node --check 全过；
- **回收站今日反复改动后契约完整**：restore/purge/deletedList 接口双侧存在且权限一致、菜单 component 路径吻合、三态快照死代码前端已清零；CMS/reader/purchase 前后端字段逐一吻合；perms 双侧抽查一致；
- **工程面**：`.env` 未进仓库（gitignore 生效，此前任务前提修正）；compose 中间件仅内网 expose；CI JDK17 与 pom 匹配；SQL 幂等设计总体良好，data_snapshot REPLACE 列数与 two_state 后全新库精确匹配不会炸初始化。

### 第五轮建议修复顺序

1. **H1** JWT 密钥（删回退+轮换）→ **H2** 升级链统一 → **H4/H7** 后台 404 与前台死点（均为小改动高收益）；
2. **H3** editor 角色 + **H5** 发布权限旁路 + **H6** dist/镜像确认；
3. M1-M3 数据守卫 + **M4** nginx body size；
4. M5-M16 与 L 组择机；
5. 公网上线前清单：频控 IP 信任链加固、admin/editor 口令更换（七节已有）、canonical 域名、H6 镜像重建确认。

# 审查记录 · 数智游民创新工场官网

> 由 `代码审查报告.md` + `审查问题清单.md` + `问题修复清单.md` 三份合并归档（2026-08-24）
> 状态：
> - 第一轮（2026-08-21 发现）：已于 2026-08-24 前全部修复（见各条目 ✅）
> - 第二轮（2026-08-24 审查 `4ccf5fb2..7562b340` 新增功能）：**新发现问题待修**，含 1 个运行时故障 + 4 个功能坏死项（见第五节 ❌）

---

## 一、修复状态总览

| 批次 | 内容 | 状态 |
|---|---|---|
| ① 功能故障 | 登录/注册参数名、goAdmin 嵌套、home 死代码、遮罩绑定、登录态字段 | ✅ 已修（`4ccf5fb2`） |
| ② 安全收口 | JWT 密钥 fail-fast、PII 快照排除、端口收窄、robots、meta 引号 | ✅ 已修（`4ccf5fb2`） |
| ③ 功能增强 | sessionToken 头化、register/apply 频控、后端文案、esc 补漏 | ✅ 已修（`4ccf5fb2` + 后续） |
| ④ 清理 | toast z-index、sticky、service 100 条上限、dist 重建、SEO 标签 | ✅ 已修（`4ccf5fb2`） |
| ⑤ 个人主页 | 注册即登录、profile.html、入驻申请挂账号、汉堡菜单 | ✅ 已修（`bf752cdc`~`c5d2cd83`） |

## 二、已修复的关键问题（记录备查）

### 功能类
1. **8 页登录/注册参数名 `name` ≠ 后端 `readerName`** —— 复制弹窗时手误，已全部改为 `readerName` ✅
2. **7 页 `goAdmin` 被嵌套进 `submitLogin`** —— 已移到脚本顶层 ✅
3. **home.html 残留 `#bookGrid` 死引用** —— 整段死代码已删，logo 改回顶 ✅
4. **7 页弹窗遮罩绑定失效**（脚本在 DOM 前）—— 改事件委托 ✅
5. **栏目页登录态显示错字段 + 无登出** —— 改 `readerName` + 补 logout ✅

### 安全类
6. **JWT 密钥默认值公开** —— `.env` 写入随机密钥 + 后端启动 fail-fast 拒绝默认值 ✅
7. **pre-commit 快照含 PII** —— 排除 reader/book_purchase_req 两表 ✅
8. **默认口令 + 端口暴露** —— compose 3306/6379 改 expose ✅
9. **CORS 全开放** —— 保留（演示环境可接受，公网部署需收窄）⚠️
10. **补办证可接管他人身份** —— 保留（演示可接受，公网需改发邮箱）⚠️
11. **configKey 匿名无白名单** —— 保留（当前键值无害）⚠️
12. **sessionToken 走 GET** —— 改 X-Session-Token 请求头 ✅
13. **register/apply 无频控** —— 加 IP 频控（失败 5 次/30 分钟）✅

### 体验类
14. **meta description 引号截断** —— 改中文引号 ✅
15. **robots.txt 全站禁抓** —— 放行前台 ✅
16. **service.html 100 条上限** —— 分页循环查找 ✅
17. **toast 层级** —— z-index 1100 ✅
18. **首页硬编码文章链接** —— 动态取政策分类最新 ✅
19. **后端图书术语残留（~40 处）** —— 统一为服务/成员/报名 ✅

### 遗留低优项（可后续处理）
- 死样式清理（8 个零引用 CSS 类，`.related-empty` 保留）
- 共享脚本抽 shared.js（当前手术式修复，未抽）
- 移动端「更多」下拉在超窄屏的布局微调

## 三、架构与技术资产（摘自代码审查报告，仍有参考价值）

### 架构总览
- **后端**：Spring Boot 3 + RuoYi 框架改造，业务模块 `ruoyi-system`（book/reader/borrow/reserve/purchase/cms/banner）
- **前台**：`ruoyi-ui/public/` 纯静态页（12 个 HTML + site.css），fetch 调 `/prod-api` 公开接口
- **后台**：Vue3 管理端（`ruoyi-ui/src/`），登录 admin/admin123（演示）
- **双会话体系**：后台 JWT（Redis 存 LoginUser）+ 前台短期会话（readerSessionService，30 分钟 TTL）

### 核心业务流程
- **报名**：`borrowByCard`（FOR UPDATE 锁 + CAS 条件更新防并发）
- **候补**：`reserveByCard`（名额满排队，`BorrowTask.reserveExpireCheck` 超时自动推进下一位）
- **入驻申请**：`purchase/apply`（登录后自动挂 reader_id，个人主页可查状态）
- **订单**：`ShopOrderServiceImpl`（状态机 0→1→2，加锁顺序 成员→服务）

### 技术实践亮点
- 前台 XSS 防护：全部插值过 `esc()` + 正文 textContent 白名单渲染
- SQL 注入：mapper 全 `#{}`，orderBy 双白名单
- 证号 SecureRandom 8 位不可预测 + 登录防枚举 + IP 频控
- 数据库快照：`.githooks/pre-commit` 自动导出（已排除 PII 表）

## 四、部署要点速查

- **本机开发**：`scripts/start-local.bat`（MySQL + 后端 8080 + 前端 8081 dev server）
- **Docker 部署**：`scripts/start-all.bat`（前端 80 端口，nginx 托管 dist）
- **数据库升级**：`sql/upgrade_2026082*.sql` 按日期顺序执行（幂等）
- **⚠️ 部署前必须重新构建 dist**（`npm run build:prod`），dist 不进 git
- **⚠️ 改动后端代码后必须重新打包并重启 java 进程**——第二轮审查的运行时故障即由"旧 jar + 新页面"版本错位引起（见五-0）

---

## 五、第二轮审查（2026-08-24，范围 `4ccf5fb2..7562b340`）

> 审查对象：个人主页体系、创客大赛页、政策咨询弹窗、A/B 类入驻类型等 12 个 commit 的前后端变更。
> 修复状态：以下问题**均未修复**（❌ 待修 / ⚠️ 接受风险）。

### 🔴 运行时故障

**R1. 前后端版本错位 → 所有需要会话的操作报"登录已失效"**（用户实机复现）
- 现象：登录成功、能打开个人主页（本地 localStorage 渲染），但报名/候补/我的报名/我的候补/改资料全部提示"登录已失效，请重新登录"，重新登录无效。
- 根因：运行中的后端是 **08-21 09:48 构建的旧 jar**（早于 `4ccf5fb2` 的会话头改造）；jar 内 `ReaderSessionService` 无 `resolveFromRequest`，只从请求参数读令牌；而新前端只发 `X-Session-Token` 头、body 不带参数 → 后端永远校验失败。
- 实机验证：同一令牌，body 参数方式通过、header 方式被拒，证据确凿。
- 影响接口：`borrow/borrowByCard`、`borrow/queryByCard`、`reserve/add|myList|cancel`、`purchase/myList`、`reader/updateMyInfo`、shopOrder 相关（共 5 个控制器）。
- 修复：重新打包 `mvn -pl ruoyi-admin -am package -DskipTests` 并重启后端；同时确认本地库已执行 `upgrade_20260824_profile.sql`（否则新 jar 起来后入驻申请模块因缺 `reader_id` 列报错，见 F3）。

### 🔴 功能坏死（新功能开箱即坏）

**F1. policy.html 咨询表单邮箱正则丢反斜杠 → 表单基本不可用**
- 位置：`ruoyi-ui/public/policy.html:248`
- `/^[w.+-]+@[w-]+(.[w-]+)+$/` 字符类只匹配字面 `w . + -`；实测 `test@example.com` → false，仅 `www@www.www` 这类能过。正常邮箱全被前端拦截。
- 其余 19 处同款正则均正确，唯此一处漏 `\`。修复：改为 `/^[\w.+-]+@[\w-]+(\.[\w-]+)+$/`。

**F2. 创客大赛「报名参赛」必然失败——bookId=23 不存在**
- 位置：`ruoyi-ui/public/contest.html:147` 写死 `CONTEST_BOOK_ID = 23`；数据库 `book` 表 ID 仅到 22（快照核实），且无任何种子 SQL 创建"创客大赛报名"服务条目，开发文档也未写部署前置步骤。
- 全新 clone + Docker 一键启动路径下该页必坏。修复：补一条种子 SQL（或后台建好后在文档/注释中固化 ID）。

**F3. 全新 Docker 部署缺 `reader_id` 列 → 入驻申请模块 SQL 报错**
- `sql/business_init.sql:441` 建表无 `reader_id` 列；`docker/mysql-init.sh` 初始化清单不含 `upgrade_20260824_profile.sql`（加列脚本）；而 `BookPurchaseReqMapper.xml` 的查询列无条件包含 `reader_id`。
- 后果：全新库初始化后，后台「入驻申请」列表与个人主页「我的入驻申请」Tab 报 `Unknown column 'reader_id'`。老库不受影响（手工跑过升级脚本）。
- 修复：`business_init.sql` 补列定义 + `mysql-init.sh` 补一行执行升级脚本。

**F4. contest.html 漏掉「注册即登录」改动**
- `e4555c9e` 给 10 个页面的注册成功回调加了写入会话（`currentUser = d.data; localStorage.setItem; renderLoginState()`），唯独 contest.html 的 `submitRegister` 成功分支未加。刚注册完点"报名参赛"仍被判未登录弹登录框。

### 🟠 中等（正确性 / 公网加固）

**M1. 挂失补办不同步候补表的证号快照**
- `ReaderServiceImpl.reissueCard()` 只更新了 `borrow_record.card_no`（updateCardNoSnapshot），没动 `book_reserve.card_no`。换新证号后：
  - 个人主页「我的候补」按新证号查 → 旧候补记录消失；
  - `reserveByCard` 重复候补校验按 card_no → 校验失效，可重复候补；
  - `cancelByCard` 用 `reserve.getCardNo()` 做归属比对 → 旧候补无法取消。

**M2. 会话过期被静默吞成"暂无记录"**
- 会话 TTL 30 分钟且无滑动续期。profile.html 的 `loadBorrows/loadReserves/loadPurchases` 与 services.html 的 `queryMyBorrow/queryMyReserves` 均不检查 `d.code`：过期后 data=null 被当空列表渲染成"暂无××记录"，用户误以为数据丢失，而非引导重登。

**M3. 频控 IP 取值可伪造**
- 登录/注册/补办/入驻申请频控基于 `IpUtils.getIpAddr()`（取 XFF 第一段），nginx `$proxy_add_x_forwarded_for` 为追加不覆盖 → 客户端自带伪造 XFF 即可轮换绕过"30 分钟 5 次"。演示可接受；公网部署需改信任 nginx 追加后的最后一段或 `$remote_addr`。⚠️

**M4. 后端 8080 直映射公网 + Swagger 匿名开放**
- compose 发布 `"8080:8080"`，同时 SecurityConfig 对 `/swagger-ui/**`、`/v3/api-docs/**` permitAll 且生产配置 `springdoc.swagger-ui.enabled: true` → 可绕过 nginx 直接浏览全量 API 文档。建议生产关 springdoc 或去掉端口映射。

### 🟡 低（打磨项）

- L1 八个页面 `renderLoginState` 结尾多拼一个孤立 `' + '</span>'`（浏览器容错忽略，复制粘贴残渣）：about/article/industry/join/news/policy/talent/service。
- L2 提交通道不一致：home/services 入驻申请走 `apiFetch`（带会话头，登录后自动挂账号）；join.html:177 与 policy.html:256 走裸 `fetch` → 登录用户提交不关联账号，「我的入驻申请」看不到自己的提交。
- L3 长度校验缺失：`book_name`(100)/`author`(60)/`remark`(255) 前端均无 maxlength，超长直接 DB 500；【A 类】前缀还会再占 remark 若干字符。
- L4 定时任务两处：`BorrowTask.reserveExpireCheck` 把候补推进为"有名额"后不发邮件（returnBook 路径发，行为不一致）；`remindOverdue` 公告文案仍是"图书/读者/办理还书"旧术语（第一轮 #19 清理的漏网）。
- L5 `FineService:45` 使用已废弃的 `BigDecimal.ROUND_HALF_UP`（应改 `RoundingMode.HALF_UP`）。
- L6 services.html 死代码：`authParams`(:227)、`PUBLIC_STATS_API`、`NOTICE_API`、`noticeTexts`、`RELATED_API` 定义后从未使用。
- L7 本地 `ruoyi-ui/dist/` 落后于 `public/`（dist 已 gitignore、Docker 构建时重建；仅本地直接开 dist 预览会看到旧版，易误判）。
- L8 news.html 注册弹窗类型下拉缺"请选择成员类型"空默认项（contest.html 有），两页不一致。

### ✅ 第二轮核对过、确认没问题的

- 第一轮已知接受风险（CORS 开放、补办可接管、configKey 匿名）维持原判；
- 报名/候补/完成的并发控制（FOR UPDATE 加锁顺序统一 成员→服务、CAS 状态推进、库存原子扣减、防双回补）依然严密；
- `.env` gitignore 生效；pre-commit 快照继续排除 PII 两表；JWT 默认密钥 fail-fast 仍生效；
- XSS 防护：12 页 `readerName` 插值全过 `esc()`；article 正文 textContent + URL 白名单方案完好；
- CI（build.yml）跑单测 + 双镜像构建验证正常。

### 建议修复顺序

1. **R1**（重新打包重启后端）+ **F3**（补列）——先让本地恢复可用；
2. **F1 / F2 / F4**——三个新功能坏死项，均为小改动；
3. **M1 / M2**——正确性问题；
4. **M3 / M4**——公网部署前必做；
5. L 组择机清理。

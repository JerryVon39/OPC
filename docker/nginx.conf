# ============================================
# 数智游民创新工场 · 前端 Nginx 配置
# 前端静态页面 + /prod-api 反向代理到后端 + /profile 封面图片
# 2026-08-24 更新：server_tokens 关闭、基础安全响应头、登录接口 IP 限流
# 说明：前台为多页静态站且依赖大量内联脚本/样式，未加严格 CSP（会破坏页面渲染）
# ============================================

# 登录接口限流（防暴力破解）：60 次/分钟/IP，突发放宽 20 次
# 覆盖后台 /login 与前台成员 /system/reader/login。
# 注意：不能设太紧（曾用 10r/m burst 5，正常登录失败几次+刷新即 503 误伤，
# 表现为"无法进入后台"）；后端已有账户锁（5 次/10 分钟）+ IP 频控兜底
limit_req_zone $binary_remote_addr zone=opc_login:10m rate=60r/m;

server {
    listen       80;
    server_name  localhost;
    # 隐藏 nginx 版本号（防版本指纹扫描）
    server_tokens off;

    # 上传体积上限（M4 修复）：默认 1MB 会拦截 >1MB 的封面/配图导致 413，
    # 与后端 max-file-size 10MB 对齐（留余量到 20m）
    client_max_body_size 20m;

    # 基础安全响应头
    add_header X-Frame-Options SAMEORIGIN always;
    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;
    # 静态资源不缓存（ETag 条件请求，更新后立即生效；本站资源小，性能影响可忽略）
    add_header Cache-Control "no-cache" always;

    # 根路径直达官网首页（后台管理走 /index 路由）
    location = / {
        return 301 /home.html;
    }

    # 前端页面（vue 构建产物）
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后台登录接口：IP 限流（防爆破）
    location = /prod-api/login {
        limit_req zone=opc_login burst=20 nodelay;
        proxy_pass http://backend:8080/login;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 前台成员登录接口：IP 限流（防爆破）
    location = /prod-api/system/reader/login {
        limit_req zone=opc_login burst=20 nodelay;
        proxy_pass http://backend:8080/system/reader/login;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 后端接口代理（前端请求带 /prod-api 前缀，去掉后转发给后端）
    location /prod-api/ {
        proxy_pass http://backend:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 上传的封面图片（后端本地存储 /profile/ 目录）
    location /profile/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;
    gzip_min_length 1k;
}

# ============================================
# HTTPS 入口（H6 修复）：公网上线前必须启用——当前仅 listen 80，
# 读者密码 / sessionToken / admin JWT 明文过网。
# 启用步骤：
#   1. 将证书放入 docker/certs/（server.crt / server.key，可用 certbot 签发）
#   2. 取消下方 443 server 块注释，并把 80 块改为 301 跳转（或两端口并存）
#   3. 如走 nginx 容器外证书（宿主机/Caddy 等），则本项目可跳过本块，
#      仅需保证外部 TLS 已终止
# ============================================
# server {
#     listen       443 ssl;
#     server_name  www.opc.example.com;
#     ssl_certificate     /etc/nginx/certs/server.crt;
#     ssl_certificate_key /etc/nginx/certs/server.key;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#     # 与 80 块相同的转发配置：根路径、静态资源、/prod-api、/profile、限流
#     location = / { return 301 https://www.opc.example.com/home.html; }
#     location / {
#         root   /usr/share/nginx/html;
#         index  index.html;
#         try_files $uri $uri/ /index.html;
#     }
#     location = /prod-api/login { limit_req zone=opc_login burst=20 nodelay; proxy_pass http://backend:8080/login; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; }
#     location = /prod-api/system/reader/login { limit_req zone=opc_login burst=20 nodelay; proxy_pass http://backend:8080/system/reader/login; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; }
#     location /prod-api/ {
#         proxy_pass http://backend:8080/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#     location /profile/ { proxy_pass http://backend:8080; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; }
# }
